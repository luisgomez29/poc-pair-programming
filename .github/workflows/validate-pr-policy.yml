name: Validate Pull Request Policy

on:
  pull_request:
    types: [opened, edited, synchronize, ready_for_review]

jobs:
  validate-policy:
    runs-on: ubuntu-latest
    if: ${{ !github.event.pull_request.draft }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Validate PR requirements
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.PULL_REQUEST_TOKEN }}
          script: |
            const pr = context.payload.pull_request;

            // Ensure PR body exists
            if (!pr.body) {
              core.setFailed("❌ The pull request body is empty.");
              return;
            }

            // Extract all issue numbers preceded by # in PR body
            const issueNumbers = Array.from(pr.body.matchAll(/#(\d+)/g), m => m[1]);

            if (!issueNumbers.length) {
              core.setFailed("❌ The pull request does not reference any issue.");
              return;
            }

            let foundOpen = false;
            let foundOpenPP = false;
            let selectedIssue = null;
            let issueAssignees = [];
            for (const issueNumber of issueNumbers) {
              const issue = await github.rest.issues.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber
              });
              const isOpen = issue.data.state === 'open';
              const hasPPLabel = issue.data.labels.some(label => label.name === 'PP');
              if (isOpen && hasPPLabel) {
                foundOpen = true;
                foundOpenPP = true;
                selectedIssue = issue;
                issueAssignees = issue.data.assignees.map(a => a.login);
                console.log(`ℹ️ Issue #${issueNumber} is open and has 'PP' label.`);
                break;
              } else if (isOpen) {
                foundOpen = true;
                console.log(`ℹ️ Issue #${issueNumber} is open and does not have 'PP' label.`);
                return;
              } else {
                console.log(`ℹ️ Issue #${issueNumber} is not open.`);
              }
            }

            if (!foundOpen) {
              core.setFailed("❌ None of the referenced issues are open.");
              return;
            }

            if (!foundOpenPP) {
              console.log("✅ At least one referenced issue is open and does not have 'PP' label.");
              return;
            }

            console.log(`ℹ️ Issue #${selectedIssue.data.number} has ${issueAssignees.length} assignee(s).`);
            issueAssignees.forEach(a => console.log(`👤 Assignee: ${a}`));

            // Validate at least two assignees
            if (issueAssignees.length < 2) {
              core.setFailed("❌ The issue with 'PP' label must have at least two assignees.");
              return;
            }

            // Identify the pair programmer (not the PR author)
            const pairReviewers = issueAssignees.filter(a => a !== pr.user.login);
            if (pairReviewers.length === 0) {
              core.setFailed("❌ Could not identify the pair reviewer.");
              return;
            }

            console.log(`Assigning reviewers from issue #${selectedIssue.data.number}:`, pairReviewers);
            await github.rest.pulls.requestReviewers({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
              reviewers: pairReviewers
            });

            console.log(`✅ The pair reviewers ${pairReviewers} have to approve the PR.`);
